I have some postprocessing script already for the latent v2 which plots the acceleration field in the case that we have a linear decoder. With our new knowledge, can we also account for if we have a nonlinear decoder? I would also like to plot a consistency check. Ideally, we could train our model with MLP encoder or GRU encoder, model our dynamics as nonlinear, linear_residual, or linear, decode using linear or MLP, and we can compare the associated errors for any combination of this. Keep things modular. Maybe we could have plots similar to the one where I show the acceleration quivers, true orbit, orbit founds only via encode and decode, and orbit found via encode, propagate, decode and each subplot can be the results for a different combination of model encoder dynamics and decoder tested on the same test orbit, with multiple plots (each plot showing a different test orbit). Ideally, we would train the models in a sweep so they would all be in the same group in wandb, and we could load the models from the wandb group to use in post analysis script. We should also pickle our results so that if we have run the analysis before, we are not repeating computations. maybe we also include a secondary option to specify run ids instead of wandb group, but this shouldn't be the focus right now